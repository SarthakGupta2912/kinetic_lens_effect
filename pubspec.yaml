name: kinetic_lens_effect
description: A beautiful kinetic dot-grid lens effect widget for Flutter. Wrap your app with LensEffect to add an animated, cursor-reactive background with zero boilerplate.
version: 1.0.0
homepage: https://github.com/SarthakGupta2912/kinetic_lens_effect
repository: https://github.com/SarthakGupta2912/kinetic_lens_effect
issue_tracker: https://github.com/SarthakGupta2912/kinetic_lens_effect/issues

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  # No platform-specific code — pure Dart/Flutter
